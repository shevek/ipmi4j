/*
 * This file is autogenerated.
 */
package org.anarres.ipmi.protocol.client;

import javax.annotation.Nonnull;
import org.anarres.ipmi.protocol.packet.asf.AsfRmcpData;
#foreach ($value in $values)
import ${value.newPacketData().class.name};
#end

/**
 * Autogenerated visitor handler for {@link AsfRmcpData} subclasses.
 *
 * @author shevek
 */
public interface IpmiClientAsfMessageHandler {

    public static class Adapter implements IpmiClientAsfMessageHandler {

        public void handleDefault(@Nonnull AsfRmcpData message) {
        }
#foreach ($value in $values)

        @Override
        public void handle${value.newPacketData().class.simpleName}(${value.newPacketData().class.simpleName} message) {
            handleDefault(message);
        }
#end
    }
#foreach ($value in $values)

    public void handle${value.newPacketData().class.simpleName}(@Nonnull ${value.newPacketData().class.simpleName} message);
#end
}
