/*
 * This file is autogenerated.
 */
package org.anarres.ipmi.protocol.client.visitor;

import javax.annotation.Nonnull;
import org.anarres.ipmi.protocol.packet.asf.AsfRmcpData;
#foreach ($value in $values)
import ${value.newPacketData().class.name};
#end

/**
 * Autogenerated visitor handler for {@link AsfRmcpData} subclasses.
 *
 * @author shevek
 */
public interface IpmiClientAsfMessageHandler {

    public static class Adapter implements IpmiClientAsfMessageHandler {

        public void handleDefault(@Nonnull IpmiHandlerContext context, @Nonnull AsfRmcpData message) {
        }
#foreach ($value in $values)

        @Override
        public void handle${value.newPacketData().class.simpleName}(IpmiHandlerContext context, ${value.newPacketData().class.simpleName} message) {
            handleDefault(context, message);
        }
#end
    }
#foreach ($value in $values)

    public void handle${value.newPacketData().class.simpleName}(@Nonnull IpmiHandlerContext context, @Nonnull ${value.newPacketData().class.simpleName} message);
#end
}
